/*
================================================================================
booleans
--------
Yes and No answers use boolean, a type that references one of two values:
true or false.

We declare boolean variables by using the keyword boolean before the variable
name.
////////////////////////////////////////////
boolean javaIsACompiledLanguage = true;
boolean javaIsACupOfCoffee = false;
////////////////////////////////////////////

In future lessons, we'll see how boolean values help navigate decisions in our
programs.

Instructions
------------
1. Create a variable called intsCanHoldDecimals. Set it to true if the int type
   can hold a decimal number. Set it to false if the int type cannot do this.

2. Print out your intsCanHoldDecimals variable.
================================================================================
*/
public class Booleans {
    public static void main(String[] args) {
        boolean intsCanHoldDecimals = false;
        System.out.println(intsCanHoldDecimals);
    }
}
